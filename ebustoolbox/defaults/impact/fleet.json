{
  "schema_version": 1,
  "_comment": [
    "Fleet topology defaults for eflips-impact. Describes the rows that must exist",
    "before a TCO calculation can run: one BatteryType per battery-electric vehicle",
    "type, and one ChargingPointType per charging type.",
    "",
    "'battery_type_default' supplies these values for every vehicle type. There is no",
    "per-vehicle-type list: django-simba fleets come from user uploads with arbitrary",
    "names, so name matching found nothing to match.",
    "",
    "'specific_mass' is in kg per kWh of gross capacity, and is the one value in these",
    "files that is not stored in a JSON column: it is BatteryType.specific_mass, which",
    "eflips-impact multiplies by the battery capacity to get the mass it charges the",
    "production emissions of. It is an input on the costs page, shared by default and",
    "settable per vehicle type, so this is what an untouched scenario uses. Note that",
    "migration 0093 created the public fleet's battery types with 2.5 instead; those",
    "rows are brought onto this value the first time a scenario reaches the LCA.",
    "",
    "ChargingPointType has no 'type' column in the database, so 'name_short' carries",
    "the depot/opportunity discriminator (see DEPOT_CPT_NAME_SHORT in impact.py)."
  ],
  "battery_type_default": {
    "specific_mass": 6.0,
    "chemistry": "lfp"
  },
  "charging_point_types": [
    {
      "type": "depot",
      "name": "Depot Charging Point",
      "name_short": "DCS"
    },
    {
      "type": "opportunity",
      "name": "Opportunity Charging Point",
      "name_short": "OCS"
    }
  ]
}
