{
  "schema_version": 1,
  "_comment": [
    "The half of the LCA parameters that is not an openLCA emission factor: values that",
    "differ per vehicle model or per charging point type. lca.json holds the rest.",
    "",
    "Shaped like tco.json, not like the file of the same name in eflips-x. That one is a",
    "list keyed by VehicleType.name_short, and eflips-impact's init_lca_params() writes",
    "nothing at all -- for any entity -- if a single battery-electric vehicle type in the",
    "scenario is missing from it. django-simba fleets come from user uploads and have",
    "arbitrary names, so a name-keyed list would match nothing and silently leave the",
    "whole scenario unparameterised. 'vehicle_type_default' therefore covers every",
    "battery-electric vehicle type, and the diesel block every diesel one.",
    "",
    "'year' selects the electricity mix from lca.json's per-year series, which drives",
    "the entire use-phase impact of a battery-electric fleet. It is a fixed constant",
    "rather than something derived from the scenario, because django-simba stores no",
    "project year anywhere. Values between the years in lca.json (2023, 2030, 2050) are",
    "interpolated; values outside that range are clamped, with a warning.",
    "",
    "'motor_rated_power_kw' is only the starting value. Motor mass is derived from it",
    "as power / power_to_weight_ratio, so one figure for the whole fleet gives an",
    "articulated bus the same motor as a 12 m one and understates its production",
    "emissions. It is therefore an input on the costs page, shared by default and",
    "settable per vehicle type, and what is here is what an untouched scenario uses.",
    "The specific mass of the battery works the same way, except that its default lives",
    "in fleet.json and its value in a column rather than in lca_parameters.",
    "",
    "Consumption is deliberately absent. eflips-impact reads it from these parameters,",
    "but ebustoolbox.impact writes it in from the simulated driving events after the",
    "run, the same value the TCO calculation uses. A number here would only be a",
    "second, unused source for it.",
    "",
    "Lifetimes are absent for the same reason. 'vehicle_lifetime_years' and",
    "'infrastructure_lifetime_years' are still required by the eflips-impact",
    "dataclasses, but ensure_lca_parameters now fills them from tco.json's 'lifetimes'",
    "block -- the single source both calculations read -- so that a bus is not written",
    "off over one number by the TCO and a different one by the LCA. They were 12.0 and",
    "20.0 here against 14 and 20 in tco.json."
  ],
  "year": 2025,
  "vehicle_type_default": {
    "motor_rated_power_kw": 200.0,
    "motor_power_to_weight_ratio_kw_per_kg": 1.5
  },
  "vehicle_type_default_diesel": {
    "motor_rated_power_kw": 200.0,
    "diesel_consumption_kg_per_km": 0.449
  },
  "charging_point_types": {
    "depot": {
      "foundation_volume_per_point_m3": 0.0
    },
    "opportunity": {
      "foundation_volume_per_point_m3": 3.96
    }
  }
}
